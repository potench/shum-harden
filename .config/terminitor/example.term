
# CHANGE this PATH to your project directory
PATH = File.expand_path("~/Sites/red/example")
ACTIVATE = "source activate"

window do
  
  before do
    run "cd #{PATH}"
  end

  run ACTIVATE
  run "rake update:git"
  run "gitx"
  
  tab do
    run ACTIVATE
    run "clear"
    run "rake watch:compass"
  end
  
  tab do
    run ACTIVATE
    run "clear"
    run "rake watch:js"
  end
  
end
