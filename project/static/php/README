# Shum-Harden Wedding Website
==============================

The .gitignore ignores index.php files (because there's 2 since statix generates one in deploy/).

Change the `index-stripped.php` file to `index.php`.  It's named as such so I don't accidentally push a file with my login-credentials to a public repo. 

### Everything else:

The Unlicense (aka: public domain)
